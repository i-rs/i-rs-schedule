[package]
edition = "2024"
name = "i-rs-schedule-cli"
version = "0.8.0"

[dependencies]
anyhow = "1"
clap = {version = "4", features = ["derive"]}
reqwest = {version = "0.13.4", features = ["json"]}
serde = {version = "1", features = ["derive"]}
serde_json = "1"
tokio = {version = "1", features = ["full"]}
uuid = "1"
