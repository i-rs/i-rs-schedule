# AGENTS.md

## Stack
- Rust, edition 2024, toolchain stable-aarch64-apple-darwin
- No dependencies, no CI, no tests

## Commands
- `cargo build` / `cargo run` / `cargo test`
- `cargo clippy` for linting (may need `rustup component add clippy`)
- `cargo fmt` for formatting (may need `rustup component add rustfmt`)
