# 产品演进路线图

**愿景**:自托管的"个人 crontab + 告警中心"——可靠、安全、日常顺手。

**迭代约定**:每个版本走既定流程(brainstorm → spec → plan → 逐 task 执行 → 冒烟 → 合并);feature 独立 commit;版本完成时打 git tag、更新 Cargo.toml 版本号与 CHANGELOG。

---

## v0.1.0 「可信」— 它挂了有人知道,不是谁都能动它

解决长期运行的三个致命缺口:demo → 可用产品的分水岭。

| 功能 | 设计要点 |
|------|----------|
| 失败通知 | 任务级通知配置;Generic Webhook + 飞书/钉钉内置格式;失败时与恢复时推送;payload 含任务名/错误输出(截断)/耗时/exec_id |
| API 认证 | `SCHEDULE_TOKEN` 环境变量;设置后全 API 要求 Bearer;未设置完全开放(本机模式,向后兼容);前端 401 → token 设置页(localStorage) |
| 执行记录保留 | `RETENTION_DAYS`(默认 30,0=永久);启动清理 + 每日定时清理 |

## v0.2.0 「顺手」— 日常使用无摩擦

| 功能 | 设计要点 |
|------|----------|
| 时区支持 | `chrono-tz`,任务级 `timezone` 字段(默认 UTC 兼容存量);cron 按任务时区计算;前端时区选择 |
| 失败重试 | 任务级 `max_retries` + 指数退避(30s→2m→8m);重试为独立 execution 并标记 attempt |
| next_run_at | API 返回下次执行时间;任务卡显示"2m 后执行" |
| per-task 超时 | `timeout_secs` 可配,默认 30 |
| 导入/导出 | JSON 全量导出/导入(不含执行历史) |

## v0.3.0 「可运营」— 部署运维标准化

Dockerfile + docker-compose → `/metrics`(Prometheus)+ `/healthz` → `config.toml` → 集成测试 + GitHub Actions

## v0.4.0 「轻编排」— 从定时任务到流水线

任务依赖链(A 成功触发 B,轻量非 DAG)→ Dashboard 统计图表 → i18n 中文

---

## 已完成基线(0.0.x)

- 核心调度:DelayQueue + cron/once、优雅停机、僵尸清理、cron 校验
- 执行器:HTTP(任意 method、30s 超时)+ Shell(30s 超时)、统一执行记录
- 存储:SQLite(r2d2 连接池、WAL、索引、事务删除)
- 接口:REST 全 CRUD + run + executions;CLI 全命令(错误退出码)
- 前端:暗色 SaaS 风格、Toast、确认框、状态过滤、详情弹窗、自动刷新、懒加载


---

# 演进路线图 · 第二期

**现状**:v0.1~v0.4 已交付(可信/顺手/可运营/轻编排)。本期主题:调试体验、编排语义、多人协作。

## v0.5.0 「洞察」— 看得更深,调得更快

| 功能 | 设计要点 |
|------|----------|
| 任务详情抽屉 | Tasks 页点击任务 → 侧滑抽屉:元信息、成功率/平均耗时统计、执行历史(状态过滤 + 分页) |
| Cron 触发预览 | 表单实时显示未来 5 次触发时间(按任务时区) |
| 通知测试按钮 | 通知配置旁"发送测试",真实 payload 打到渠道并反馈成败 |
| 时长趋势线 | 14 天图表叠加平均耗时折线 |

## v0.6.0 「编排补全」— 把链做完整

| 功能 | 设计要点 |
|------|----------|
| 触发策略 | trigger_on: success(默认)/ failure / always |
| 多下游 | trigger_task_ids 数组,一触多 |
| 上下游传参 | 下游命令支持 {{trigger.output}} / {{trigger.status}} 插值 |
| 任务克隆 | 复制全部配置为新任务 |

## v0.7.0 「多人」— 从个人工具到团队工具

登录会话(用户名+密码,与 token 并存)→ Token 管理界面(哈希存储)→ 审计日志 → 通知渠道全局默认

---

# 演进路线图 · 第三期

**现状**:v0.5~v0.7 已交付(洞察/编排补全/多人)。本期主题:把"单人试用"推向"团队日常依赖"——执行过程透明、任务规模不失控、事件驱动、无人值守也可信。

**对原 v0.8「规模化」的重定界**:Postgres/多实例对单机自托管定位价值有限且工程量大,降为远期;Shell 实时输出流与 PWA 提前并入本期 v0.8。

## v0.8.0 「实时」— 执行不再是黑盒

| 功能 | 设计要点 |
|------|----------|
| Shell 实时输出流 | 执行中任务的输出实时上屏(长轮询,desirable 响应体为 Full\<Bytes\> 不支持流式 SSE):后端为每个 running execution 维护有界累积缓冲(head 32KB + tail 滚动 32KB)+ watch 变更通知;任务抽屉内嵌终端风格实时视图;完成后落库 |
| 输出持久化上限 | `MAX_OUTPUT_KB`(默认 64,0=不限):HTTP body 与 shell 输出超限截断(保留头尾),防 SQLite 膨胀;通知 payload 截断沿用 |
| 推送式刷新 | 执行列表/任务抽屉改事件长轮询订阅(新执行、状态变化即时上屏),断线自动退避重连;Dashboard 30s 轮询退役 |
| PWA | manifest + 最小 service worker:可安装到手机桌面、应用图标、壳层离线 |

鉴权:长轮询走现有 Auth 中间件(Bearer header / cookie 会话直接可用),无需为流连接单独发 ticket。

## v0.9.0 「秩序」— 任务多了、跑久了,不失控

| 功能 | 设计要点 |
|------|----------|
| 并发与互斥 | 任务级 `max_concurrent`(默认 1):上轮未结束 → 跳过并记录 `skipped` 状态(防重复执行的破坏性,如重复扣款/通知),可选 queue 模式;全局并发上限信号量兜底 |
| 标签系统 | 任务多标签、按标签过滤、批量启用/禁用/删除;标签染色 |
| 维护模式 | 一键全局暂停调度(执行中的不中断);暂停期间 cron 不发放、once 到期记 `skipped(missed)` 不补偿;恢复后按 next_run 续跑;顶栏横幅提示 |
| 自动备份 | 每日 `VACUUM INTO` 到备份目录(`BACKUP_DIR`/`BACKUP_KEEP` 保留份数);healthz 展示最近备份时间 |

## v0.10.0 「事件」— 不止定时:外部世界也能触发

| 功能 | 设计要点 |
|------|----------|
| Webhook 触发器 | 任务可开启触发端点 `/api/hooks/:task_id/:secret`(secret 哈希存储);POST 即执行,支持 `{{event.body}}` / `{{event.query.x}}` 插值;复用并发策略与审计 |
| 变量与密钥 | 全局与任务级变量;密钥类型写入后接口打码、导出自动脱敏、仅执行时插值;URL/headers/shell 统一支持 `{{var.name}}` |
| 漏跑检测 | 任务可开启"未按时执行告警":next_run_at 已过 + 宽限期内无 success → 推送告警;覆盖进程崩溃/重启窗口/误禁用等"沉默故障" |

## v0.11.0+ 「远期」— 按真实需求启动

- Postgres 可选后端(db 层抽象;仅当出现多实例需求)
- 多实例 leader 选举(依赖 Postgres)
- 日历视图(未来 7 天排程时间线,洞察补全)
- OAuth/LDAP 登录、执行输出全文检索
